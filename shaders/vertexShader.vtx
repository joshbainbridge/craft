#version 150

in vec3 position;
in vec3 color;
in vec3 centre;

out vec3 Color;

uniform mat4 rotate;
uniform mat4 view;
uniform mat4 proj;

void main()
{
	Color = color;
    gl_Position = proj * view * rotate * vec4(position + centre, 1.0);
}