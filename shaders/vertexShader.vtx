#version 150

in vec3 position;
in vec3 color;
in vec3 centre;

out vec3 Color;

uniform mat4 view;
uniform mat4 proj;

void main()
{
	Color = vec3((centre[0]) / 128, (centre[1]) / 128, (centre[2]) / 128);
    gl_Position = proj * view * vec4(position + centre, 1.0);
}