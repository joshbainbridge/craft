#version 150

in vec3 position;
in vec3 color;
in vec3 centre;

out vec3 Color;

uniform mat4 view;
uniform mat4 proj;

void main()
{
	Color = vec3(0.5, 0.1, (centre[2] + 8) * 0.05);
    gl_Position = proj * view * vec4(position + centre, 1.0);
}